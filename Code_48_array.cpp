#include<stdio.h>
int main()
{
	int a[50],n,i,pos=0,key=0;
	printf("Enter the size of array: ");
	scanf("%d",&n);
	printf("Enter elements of array: ");
	scanf("%d",&key);
	printf("Enter the element you want to find: ");
	scanf("%d",&)
	for(i=0;i<=n-1;i++)
	{
		if(a[i]==key)
		{
			(s=i);
		}
		printf("element=%d position=%d");
	}
}
